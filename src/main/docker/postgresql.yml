version: '2'
services:
  clientservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/clientService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=clientService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
